server:
  port: 8089

spring:
  output:
    ansi:
      enabled: always
  application:
    name: Exam7

  datasource:
    url: jdbc:h2:./db/exam7;AUTO_SERVER=TRUE
    username: Denis
    password: admin
    driver-class-name: org.h2.Driver

  liquibase:
    change-log: classpath:/db/changelog/master.yaml

logging:
  level:
    root: info

  file:
    path: log